# Pip fallback for Colab / Kaggle, where conda is unavailable.
# Kept in lockstep with environment.yml — change both together.

# pandas/pyarrow/numpy are pinned to the versions the pipeline is actually
# tested against, not the newest available.
pandas==2.3.3
pyarrow==21.0.0
numpy==2.3.5

# Lexical retrieval
bm25s==0.3.10
nltk==3.10.1

# Semantic retrieval. torch and transformers are what embed.encode runs on;
# it only ever runs on a hosted GPU, but they are declared here because the
# pooling it does is tested locally.
faiss-cpu==1.15.0
transformers==5.14.1
torch==2.13.0

# Metrics
scikit-learn==1.9.0

# Data acquisition
huggingface-hub==1.26.0
gdown==6.1.0
requests==2.34.2

tqdm==4.70.0
pytest==9.1.1
